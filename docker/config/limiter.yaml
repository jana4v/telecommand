service:
  name: tm-limiter
  log_level: info

redis:
  addr: "redis:6379"
  password: ""
  db: 0

database:
  type: "postgres"
  dsn: "postgres://scg:Checkout123@postgres:5432/scg?sslmode=disable"

poll_interval_ms: 500
