service:
  name: tm-comparator
  log_level: info

redis:
  addr: "redis:6379"
  password: ""
  db: 0

database:
  type: "postgres"
  dsn: "postgres://scg:Checkout123@postgres:5432/scg?sslmode=disable"

compare_pairs:
  - chain1: TM1
    chain2: TM2

poll_interval_ms: 500
