server:
  host: "0.0.0.0"
  port: 21002

redis:
  addr: "redis:6379"
  password: ""
  db: 0

database:
  type: "postgres"
  dsn: "postgres://scg:Checkout123@postgres:5432/scg?sslmode=disable"

umacs:
  # Update tc_ip and data_server_ip to the real UMACS hardware addresses.
  # Set tc_ip to "127.0.0.1" to use the built-in emulator instead.
  tc_ip: "172.20.xx.xx"
  tc_port: "8787"
  data_server_ip: "172.20.xx.xx"
  api_req_source: "pcc"
  api_req_priority: "normal"
  api_req_execution_mode: "auto"
  api_req_subsystem: "payload"

emulator:
  queued_delay_ms: 500
  in_progress_duration_ms: 4000
  success_rate: 100
  validate_required: true
