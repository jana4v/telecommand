service:
  name: "iam"
  log_level: "info"

http:
  port: 21005

postgres:
  dsn: "postgres://scg:Checkout123@postgres:5432/scg?sslmode=disable"

iam:
  # Change jwt_secret to a strong random value before first deployment.
  jwt_secret: "CHANGE_ME_use_a_strong_secret_in_production_min32chars"
  admin_password: "Admin@123!"
